# Inventory and Traceability API

This directory contains a minimal Flask RESTful API for managing items, lots, and batches for traceability. It uses SQLite and SQLAlchemy.

## Usage

Install dependencies:
```bash
pip install -r requirements.txt
```

Run the server:
```bash
python app.py
```
